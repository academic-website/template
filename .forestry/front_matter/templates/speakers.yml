---
label: Speakers
hide_body: false
is_partial: true
fields:
- type: field_group_list
  name: speakers
  label: Speakers
  description: List prominent speakers
  fields:
  - type: file
    name: speaker_photo
    label: Speaker photo
    config:
      maxSize: '1'
  - type: text
    name: speaker_name
    label: Speaker name
    config:
      required: true
  - type: text
    name: speaker_title
    label: Speaker title/organisation
  - type: textarea
    name: speaker_bio
    label: Speaker bio
    config:
      wysiwyg: true
      schema:
        format: markdown
- type: text
  name: block
  label: block
  hidden: true
  default: speaker-list
- type: select
  name: display
  config:
    source:
      type: simple
    options:
    - One per row
    - Two per row
    - Five max per row
  label: Display
  description: Choose display format
